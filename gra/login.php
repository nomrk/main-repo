<?php
	$db_hostname = 'localhost';
	$db_database = 'game';
	$db_username = 'phpuser';
	$db_password = 'florinagratis';
	
	//~ $sqli = new mysqli($db_hostname, $db_username, $db_password, $db_database);
	//~ if($sqli->connect_error)
		//~ die($sqli->connect_error);
	
	//ini_set('session.gc_maxlifetime', 60*60*24); set session life time to 24h REMEMBER JUST IN CASE 
	//dunno where to put it so that i will find it when i need it so i don't forget it 
	//$_SESSION['ip'] = $_SERVER['REMOTE_ADDR'];
	//if($_SESSION['ip'] != $_SERVER['REMOTE_ADDR'];
	//	different user 
	// another security check for sessions without SSL 
?>
